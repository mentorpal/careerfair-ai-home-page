{
  "name": "gatsby-template-project",
  "version": "1.0.0",
  "private": true,
  "description": "gatsby-template-project-client",
  "keywords": [
    "gatsby"
  ],
  "scripts": {
    "format": "prettier --write '**/*.{js,jsx,json,ts,tsx}'",
    "develop": "gatsby develop",
    "start": "gatsby develop",
    "build": "npx gatsby build --prefix-paths",
    "serve": "gatsby serve --prefix-paths",
    "clean": "gatsby clean",
    "test:audit": "npm audit --production --audit-level=moderate",
    "test:lint": "eslint src --ext ts --ext tsx --ext js --ext jsx --max-warnings 0",
    "test:types": "tsc --noEmit"
  },
  "dependencies": {
    "gatsby": "^3.13.0",
    "react": "^17.0.1",
    "react-dom": "^17.0.1"
  },
  "devDependencies": {
    "eslint-config-prettier": "^8.3.0",
    "typescript": "^4.4.3"
  }
}
